{
  "name": "create-todo-in-todos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "todos/create-todo/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/todos/create-todo",
        "main": "todos/create-todo/src/index.ts",
        "tsConfig": "todos/create-todo/tsconfig.lib.json",
        "assets": []
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "todos/create-todo/jest.config.ts"
      }
    }
  },
  "tags": []
}
