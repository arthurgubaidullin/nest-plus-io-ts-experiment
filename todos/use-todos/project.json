{
  "name": "use-todos-in-todos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "todos/use-todos/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/todos/use-todos",
        "main": "todos/use-todos/src/index.ts",
        "tsConfig": "todos/use-todos/tsconfig.lib.json",
        "assets": []
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "todos/use-todos/jest.config.ts"
      }
    }
  },
  "tags": ["tier:api"]
}
